export type CitaType = {
  title: string;
  fecha: string;
  hora: string;
  descripcion: string;
  direccion: string;
};

export type ErrorType = {
  tramite?: string;
  title?: string;
  titulo?: string;
  fecha?: string;
  hora?: string;
};
