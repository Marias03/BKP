export type CitaType = {
  title: string;
  fecha: string;
  hora: string;
  descripcion: string;
  direccion: string;
};

export type ErrorType = {
  titulo?: string;
  fecha?: string;
  hora?: string;
};
